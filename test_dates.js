const { formatDateTime, formatLongDateTime, formatDate } = require('./resources/js/utils/formatters.js');
// Wait, they are ES modules. We can use node --experimental-modules or just read the file and eval
